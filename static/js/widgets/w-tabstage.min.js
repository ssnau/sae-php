
Tabstage=function(id,cfg){id=/^#/.test(id)?id:'#'+id;var prevId=id+" ";var ndStage=$(id);if(!ndStage.length)return false;var ndHead=$(prevId+'.hd ul');var ndTabs=$(prevId+'.hd ul li');var ndPages=$(prevId+'.body .page');cfg=!!cfg?cfg:{};var config=$.extend({},cfg);ndHead.delegate('li','click',function(e){ndTabs.removeClass('active');$(this).addClass('active');var page=this.getAttribute('data-page');ndPages.removeClass('active');for(var i=0;i<ndPages.length;i++){elem=ndPages[i];if(elem.getAttribute('data-name')==page){$(elem).addClass('active');}}});}